# 500Hz 动态生成报告示例

## 场景说明
用户录音频率为 500Hz，与种子库中所有锚点相似度均 < 85%，系统自动通过逻辑矩阵生成分析报告。

---

## 能量特征计算

### 脉轮能量分布
基于 500Hz 核心频率的分析：

```json
{
  "root": 0.45,      // 海底轮 (194Hz) - 较弱
  "sacral": 0.52,    // 脐轮 (417Hz) - 中等
  "solar": 0.95,     // 太阳轮 (528Hz) - 极强 ⭐ 主导
  "heart": 0.68,     // 心轮 (343Hz) - 中上
  "throat": 0.55,    // 喉轮 (384Hz) - 中等
  "thirdEye": 0.42,  // 眉心轮 (432Hz) - 较弱
  "crown": 0.38      // 顶轮 (963Hz) - 弱 ❌ Gap
}
```

### 质地与相位检测
- **纹理质地**: Smooth (流畅)
- **相位模式**: Grounded (扎根向下)

---

## 自动生成标签

### 标签生成逻辑
根据公式：`[主导脉轮色描述] + [纹理特质] + [相位流向身份]`

**生成结果**:
```
黄色·流畅的表达者·扎根者
```

### 色彩编码
系统自动分配太阳轮对应色彩：
- **主色**: `#FFD700` (金黄色)
- **用于**: UI主题色、标题、渐变背景

---

## 身心建议自动化

### 脏腑定位
基于太阳轮物理区间 (500-550Hz)：
```
脾、胃功能活跃，大脑需关注
```

### 能量补给建议
- **Gap脉轮**: 顶轮 (crown)
- **推荐充电频率**: 963Hz
- **补充理由**: 能量过度聚集于下三轮，上三轮（尤其顶轮）能量断层严重

### 行为指导

#### ✅ 建议做
- 目标设定
- 意志力训练
- 晒太阳

#### ❌ 避免做
- 过度消耗
- 忽视休息
- 忽视灵性

---

## 文案话术转换

### 原始分析（物理语言）
> "核心频率500Hz，太阳轮能量极强，顶轮能量不足，相位呈下沉态。"

### 转换后（禅意话术）
> "您正处于太阳轮通达的状态。能量如同正午的阳光，意志力充沛，执行力超群。建议将一部分能量提升至顶轮，补充灵性连接，让物质与精神达到平衡，您将获得更完整的生命力。"

---

## 完整报告预览

### UI 显示效果

```
┌─────────────────────────────────────┐
│  【 黄色·流畅的表达者·扎根者 】      │
│                                     │
│  动态生成 DYN500 · 匹配度：0%       │
│                                     │
│  物理定义：核心频率500Hz(太阳轮)，  │
│  能量流转顺畅，表达清晰有力，       │
│  能量向下扎根。                     │
│                                     │
│  体感意义：物质显化力强，火元素主导。│
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  脏腑对应：脾、胃功能活跃，大脑需关注 │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  充电频率·963Hz                      │
│                                     │
│  [播放调频音频] 🎵                   │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  建议做                              │
│  [目标设定] [意志力训练] [晒太阳]    │
│                                     │
│  避免做                              │
│  [过度消耗] [忽视休息] [忽视灵性]    │
└─────────────────────────────────────┘
```

---

## 技术实现细节

### 1. 逻辑矩阵计算
```typescript
// 主导脉轮自动识别
const dominantChakra = getDominantChakra(chakraEnergy);
// → "solar" (太阳轮)

// Gap脉轮自动计算
const gapChakras = getGapChakras(chakraEnergy);
// → ["crown", "thirdEye"]

// 相位自动判断
const phase = calculatePhase(chakraEnergy);
// lowerEnergy = 0.64, upperEnergy = 0.45
// → "grounded" (扎根态)
```

### 2. 标签自动组装
```typescript
const chakraInfo = CHAKRA_MATRIX['solar'];
// colorDesc: "黄色"

const textureInfo = TEXTURE_DESCRIPTORS['smooth'];
// personality: "流畅的表达者"

const phaseInfo = PHASE_DESCRIPTORS['grounded'];
// identity: "扎根者"

const tagName = `${chakraInfo.colorDesc}·${textureInfo.personality}·${phaseInfo.identity}`;
// → "黄色·流畅的表达者·扎根者"
```

### 3. 建议自动生成
```typescript
// 脏腑映射
const organs = `${chakraInfo.organs.slice(0, 2).join('、')}功能活跃，${gapChakraInfo.organs[0]}需关注`;
// → "脾、胃功能活跃，大脑需关注"

// 充电频率
const rechargeHz = gapChakraInfo.frequency;
// → 963 (顶轮频率)

// 行为列表
const doList = generateDoList('solar', 'grounded', 'smooth');
// → ["目标设定", "意志力训练", "晒太阳"]

const dontList = generateDontList('solar', 'grounded', 'smooth');
// → ["过度消耗", "忽视休息", "忽视灵性"]
```

---

## 与种子锚点的对比

| 特征维度      | 500Hz 动态生成   | 种子 528 (太阳神) | 相似度  |
|-------------|-----------------|------------------|--------|
| 核心频率     | 500Hz           | 528Hz            | 94.7%  |
| 主导脉轮     | 太阳轮(0.95)    | 太阳轮(1.00)     | 95%    |
| 相位模式     | Grounded        | Grounded         | 100%   |
| 纹理质地     | Smooth          | Smooth           | 100%   |
| **总相似度** | -               | -                | **82%** |

**结论**: 未达到 85% 阈值，使用动态生成报告，而非匹配种子 528。

---

## 系统优势

### 1. 全覆盖
- **种子库**: 21个精准锚点（相似度 ≥ 85%）
- **动态生成**: 384种理论分型 = 7脉轮 × 3相位 × 3纹理 × 频率连续值

### 2. 零误差
- 即使用户频率为罕见值（如 287Hz、771Hz），系统依然可以生成有效报告
- 不存在"无法识别"的情况

### 3. 持续学习
- 管理员可将高质量动态生成报告提升为锚点
- 系统自动优化，种子库不断扩充

---

**生成时间**: 2026-02-26
**系统版本**: 2026.1.0
**逻辑引擎**: dynamicPrototypeGenerator.ts
